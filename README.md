# js-linked-list

Linked list data structure in JavaScipt.

### Create list
`let ll = new LinkedList()`

### Use methods
`ll.size()`

`ll.addToHead(value)`

`ll.addToTail(value)`

`ll.removeHead()`

`ll.removeTail()`

`ll.search(value)`

`ll.print()`